{
  "mcpServers": {
    "tessera_memory": {
      "command": "uvx",
      "args": ["--from", "tessera-mcp", "tessera-mcp"],
      "env": {
        "TESSERA_API_KEY": "${TESSERA_API_KEY}",
        "TESSERA_REPO": "${TESSERA_REPO}"
      }
    }
  }
}
